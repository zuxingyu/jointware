{
    "apiVersion": "v1",
    "kind": "PersistentVolumeClaim",
    "metadata": {
       "name": "registry-pvclaim"
  },
  "spec": {
     "accessModes": ["ReadWriteMany"],
     "resources": {
        "requests": {
           "storage": "399Gi"
      }
    }
  }
}

